"use strict";

const { onRequest } = require("firebase-functions/v2/https");
const app = require("../app");

exports.api = onRequest(
  {
    region: "asia-south1",
    cors: false,
    maxInstances: 10
  },
  app
);
